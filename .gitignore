# Build output. This file exists because its absence was not theoretical: the
# first publication of this repository carried 203 files of compiled classes and
# Gradle caches, because the mirror had been built in before anything ignored
# them. Nothing here is generated by the sync — it all comes from running Gradle
# in this working copy.
build/
*/build/
.gradle/
*/.gradle/

# Loom's development runtime: worlds, logs, configs from testing the mod.
run/
runs/
*/run/
*/runs/

# Editors and OS noise.
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db
